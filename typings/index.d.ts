import { IComponentData, MiniComponent as WeappMiniComponent, method, pageLifetime, lifetimes, observer, IMiniEvent } from "mipp";
import { MiniComponent as AliMiniComponent } from "mipp-ali";
import ComponentBase from "./ComponentBase";
export { ComponentBase, WeappMiniComponent as MiniComponent, WeappMiniComponent, AliMiniComponent, IComponentData, observer, method, pageLifetime, lifetimes, IMiniEvent, };
